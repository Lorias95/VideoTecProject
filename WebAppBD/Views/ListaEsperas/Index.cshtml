@model IEnumerable<WebAppBD.Models.ListaEspera>

@{
    ViewData["Title"] = "Index";
}

<h1>Registros de Lista en Espera</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Nuevo Registro</a>
</p>
<table class="table table-striped" color="blue">
    <thead>
        <tr>
            <th>
                Cantidad Socios
            </th>
            <th>
                Tiempo Espera
            </th>
            <th>
                IdFicha
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadSocios)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TiempoEspera)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdFichaNavigation.Pelicula)
            </td>
            <td>
                <a class="btn btn-success" a asp-action="Edit" asp-route-id="@item.IdListaEspera">Edit</a> |
                <a class="btn btn-warning" a asp-action="Details" asp-route-id="@item.IdListaEspera">Details</a> |
                <a class="btn btn-danger" a asp-action="Delete" asp-route-id="@item.IdListaEspera">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
