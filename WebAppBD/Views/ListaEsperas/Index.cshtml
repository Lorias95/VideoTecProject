@model IEnumerable<WebAppBD.Models.ListaEspera>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CantidadSocios)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TiempoEspera)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdFichaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadSocios)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TiempoEspera)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdFichaNavigation.Pelicula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdListaEspera">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdListaEspera">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdListaEspera">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
